# Dependencies
node_modules/
.pnpm-store/

# Next.js
.next/
out/
build/
dist/

# Production
*.tsbuildinfo
next-env.d.ts

# Testing
coverage/
.nyc_output/

# Misc
.DS_Store
*.pem
.vscode/
.idea/
*.swp
*.swo
*~

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local env files
.env
.env*.local
.env.development
.env.production

# Vercel
.vercel

# Supabase
supabase/.branches
supabase/.temp

# Drizzle
apps/web/drizzle/meta/

# OS
Thumbs.db

# Logs
logs/
*.log

# Expo / React Native (apps/mobile)
.expo/
.expo-export/
android/
ios/
*.keystore
!debug.keystore
expo-env.d.ts

# Personal learning notes — not part of the project
learn/

# Database dumps from `pnpm db:backup`. Never commit these — they contain
# every ball of every match, and on a real database, real people.
backups/

# Local agent tooling — installed per-machine with `npx skills add`, not part
# of the project. Kept out so a clone is not obliged to adopt one person's
# assistant setup, and so the repo stays buildable with nothing but pnpm.
.claude/
skills-lock.json


# Personal trackers — infrastructure notes, account maps, private planning.
# These map accounts and services and are deliberately not part of the repo.
WIRING.md
ignore_its_personal.md
new-design

# The pivot audit's checklist. All 61 findings are closed and the work is in
# master, so what it now reads as is an open work order for something already
# shipped. Kept on disk because its last three sections are still live — the
# unfinished type scale, the primary button under AA, and the fact that none
# of it has been seen on a screen.
PIVOT-TRACKER.md
