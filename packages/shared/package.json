{
  "name": "@open-innings/shared",
  "version": "0.1.0",
  "private": true,
  "description": "API contract shared by the web backend and the mobile client — enums, Zod schemas, DTOs, and isomorphic helpers. No I/O, no database driver.",
  "license": "AGPL-3.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
